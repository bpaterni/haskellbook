-- The Eq class
-- c) maces equality tests possible

-- The type class Ord
-- b) is a subclass of Eq

-- Suppose the type class Ord has an operator >. What is the type of >?
-- a) Ord a => a -> a -> Bool

-- In x = divMod 16 12
-- c) the type of x is a tuple

-- The type class Integral includes
-- a( Int and Integer numbers
